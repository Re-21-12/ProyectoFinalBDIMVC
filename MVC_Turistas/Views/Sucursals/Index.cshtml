@model IEnumerable<MVC_Turistas.Models.Sucursal>

@{
    ViewData["Title"] = "Inicio";
}

<h1>Inicio</h1>

<p>
    <a asp-action="Create" class="btn">Crear Nuevo</a>
</p>
<table class="table table-hover border-warning table-borderless">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
            <td>
                <a asp-action="Edit" class="btn-edit"  asp-route-id="@item.CodigoSucursal">Editar</a> |
                <a asp-action="Details" class="btn-detail"  asp-route-id="@item.CodigoSucursal">Detalle</a> |
                <a asp-action="Delete" class="btn-delete" asp-route-id="@item.CodigoSucursal">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
